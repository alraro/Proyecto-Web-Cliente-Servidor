{
  "permissions": {
    "allow": [
      "Bash(mvn compile *)",
      "Bash(curl -sc /tmp/cookies.txt -X POST http://localhost:8080/login -d \"email=franjimenez@bancosol.info&password=changeme\" -w \"%{redirect_url}\" -o /dev/null)",
      "Bash(curl -sb /tmp/cookies.txt -sv http://localhost:8080/coordinator-campaigns)",
      "Bash(docker exec postgres-database psql -U postgres -c ' *)",
      "Bash(docker exec *)"
    ]
  }
}
